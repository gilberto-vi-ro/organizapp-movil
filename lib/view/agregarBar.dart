import 'package:flutter/material.dart';

class PaginaAgregarBar extends StatelessWidget {
  const PaginaAgregarBar({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Bienvenido a agregar"),
      ),
    );
  }
}
