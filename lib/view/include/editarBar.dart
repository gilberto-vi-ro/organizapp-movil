import 'package:flutter/material.dart';

class PaginaEditBar extends StatelessWidget {
  const PaginaEditBar({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Bienvenido a editar"),
      ),
    );
  }
}
